// This component has been superseded by the ListingBrowser component.
// Keeping the file to avoid import errors in case it's referenced elsewhere.
// The ListingBrowser component provides inline listing import with promo note generation.

export {};
